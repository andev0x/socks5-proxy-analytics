
proxy:
  address: "0.0.0.0"
  port: 1080
  auth:
    enabled: false
    username: "user"
    password: "pass"
  max_connections: 10000
  ip_whitelist: []

api:
  address: "0.0.0.0"
  port: 8080

database:
  host: "localhost"
  port: 5432
  user: "anvndev"
  password: ""
  database: "socksdb"
  sslmode: "disable"

pipeline:
  workers: 4
  buffer_size: 10000
  batch_size: 100
  flush_interval_ms: 5000

logging:
  level: "info"
  format: "json"

rate_limit:
  enabled: false
  requests_per_second: 100
